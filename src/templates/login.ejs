<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= __('authorization') %> | <%= HOST %></title>
</head>
<body>
<% include widgets/header.ejs %>

<div class="container">
    <div class="content d-flex justify-content-center">
        <div class="fine-wrapper text-left bg-semi-black">
            <div class="app-title">
                <h1 class="text-center">
                    <%= __('authorization') %>
                </h1>
            </div>
            <div class="d-flex flex-column justify-content-center">
                <div class="d-flex flex-row justify-content-center  align-items-center token-for-enter mb-4">
                    <p class="m-0 mr-2"><%=  __('token-for-enter') %></p>
                    <div class=" white-tooltip info-icon" data-toggle="tooltip" data-placement="top" title="<%= __('token-for-enter-info') %>"></div>
                </div>
                <div class="d-flex justify-content-center" id="login">
                </div>
                <div class="dont-have-account mt-4">
                    <p><%= __('dont-have-account')%>
                        <br/>
                        <a href="/registration"><%= __('register-now') %></a>
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>

<% include widgets/footer.ejs%>
</body>
</html>